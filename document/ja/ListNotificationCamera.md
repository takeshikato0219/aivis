# ListNotificationCamera

## 1. 目次

1. [目次](#1-目次)
2. [コンポーネント](#2-コンポーネント)
3. [ナビゲーション](#3-ナビゲーション)
4. [ロジック・API](#4-ロジックapi)

## 2. コンポーネント

| 名前 | 種類 | 必須 | デフォルト | 型 | バリデーション・備考 |
|------|------|------|------------|-----|----------------------|
| `title` | ルート | はい | — | `string` | ヘッダータイトル |
| `icon` | ルート | はい | — | `string` | アイコンマップ名 |
| `code` | ルート | はい | — | `string` | ルール／通知種別コード |
| `cameraId` | ルート | はい | — | `string` | |
| `detected_at` | ルート | いいえ | — | `string` | 日付フィルタ用 |
| 日付切替 | 前後ナビ | — | — | — | 実装内で前日／翌日 |

## 3. ナビゲーション

- **遷移元:** `Detail`, `Notifications` 等。
- **戻る:** `goBack()`。

## 4. ロジック・API

- `notificationsService.getNotificationWithType(...)` — カメラ + 種別コード + 日付で一覧取得。
