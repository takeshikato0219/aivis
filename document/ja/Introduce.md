# Introduce

## 1. 目次

1. [目次](#1-目次)
2. [コンポーネント](#2-コンポーネント)
3. [ナビゲーション](#3-ナビゲーション)
4. [ロジック・API](#4-ロジックapi)

## 2. コンポーネント

| 名前 | 種類 | 必須 | デフォルト | 型 | バリデーション・備考 |
|------|------|------|------------|-----|----------------------|
| Logo | SVG | — | — | — | タブレット／スマホでサイズ変更 |
| `FlatList` スライド | リスト | — | `slides`（1 件） | `key`, `image` | 横スクロール、paging、`scrollEnabled={false}` |
| ホームへボタン | `TouchableOpacity` | — | — | — | `goToHome` |
| `ImageBackground` | webp アセット | — | — | — | 全画面背景 |

## 3. ナビゲーション

- **スタック:** `App` — ログイン後の最初の画面の場合あり（フローにより `Home` の前）。
- **遷移先:** `navigation.navigate('Home')`。

## 4. ロジック・API

- API 呼び出しなし。`onLayout`、横向き／`isTablet()` に応じてスライドサイズを計算。
