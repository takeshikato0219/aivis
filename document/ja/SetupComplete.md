# SetupComplete

## 1. 目次

1. [目次](#1-目次)
2. [コンポーネント](#2-コンポーネント)
3. [ナビゲーション](#3-ナビゲーション)
4. [ロジック・API](#4-ロジックapi)

## 2. コンポーネント

| 名前 | 種類 | 必須 | デフォルト | 型 | バリデーション・備考 |
|------|------|------|------------|-----|----------------------|
| `cameraName` | ルート | はい | — | `string` | 完了画面の表示 |
| `ssid` | ルート | はい | — | `string` | |
| アニメーション | `Animated` | — | — | — | アイコンの scale + fade |
| ダッシュボードへ | ボタン | — | — | — | → `Home` |
| QR スキャンを続ける | ボタン | — | — | — | → `QRScanner` |

## 3. ナビゲーション

- **パラメータ:** `SetupComplete`: `{ cameraName, ssid }`。
- **遷移先:** `Home`, `QRScanner`。

## 4. ロジック・API

- API 呼び出しなし。`CameraSetup` 後の確認 UI のみ。
